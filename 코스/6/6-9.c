#include <stdio.h>
#include <string.h>

int main(void) {
  //회문 비교해야 하는데
  // 회문은 sns처럼 중간 기준으로 앞뒤가 똑같은거다
  //입력받는 문장에서 알파벳만 골라서 새로운 문자열 만들고
  //이 문자열에 대해 양쪽 끝에서 가운데까지 각각의 모든 문자가 같은지
  
  return 0;
}
